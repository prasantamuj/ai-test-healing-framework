# Default/local environment configuration for api-tests-java.
# Override any key at runtime with -Dkey=value, or with an env-<name>.properties file selected
# via -Dtest.env=<name> / TEST_ENV=<name>. Never put a real secret/token here — use
# API_AUTH_TOKEN as an environment variable instead (see ApiConfig.authToken()).

base.url=https://jsonplaceholder.typicode.com
request.timeout.ms=10000
auth.token=
